spring:
  output:
    ansi:
      enabled: always
  security:
    user:
      name: admin
      password: admin
# Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:3234/eureka/
    register-with-eureka: false
    fetch-registry: false
server:
  port: 4234
# jwt
jwt:
  secret: supersecretkey
  tokenPrefix: Bearer
